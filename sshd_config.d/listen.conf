# Installed by ../setup into $PREFIX/etc/ssh/sshd_config.d/listen.conf.
# Edit this file (not the live copy) and re-run `bash setup`.

Port 8022
ListenAddress 0.0.0.0
PubkeyAuthentication yes
PasswordAuthentication no
PermitRootLogin no
